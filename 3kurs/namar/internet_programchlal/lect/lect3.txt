hash(mt5)-decript hiij boldoggui



increption-decript hiij bolno





SELECT COUNT(*) as too, MIN(email) as email 
FROM t_user
WHERE email='sarnai@gmail.com' and ustatus>=1 



INSERT INTO t_user
VALUES(default,
	   'boldssoo@gmail.com',
	  'ccc',
	  'dd',
	  '1111111',
	  'hello',
	  NOW(),
	  NOW(),
	 'autcode222',
	  NOW()+INTERVAL '1 day',
	 0
	  )



SELECT COUNT(*) 
FROM t_user
WHERE authcode='autcode222' and authendate>NOW() 

UPDATE t_user
SET ustatus=1, authcode='dsds', authendate='1900-01-01'
WHERE authcode='1' and authendate>NOW() 


UPDATE t_user
SET authendate='1900-01-01',ustatus=-1,authcode='dsds'
WHERE email=email and ustatus!=1



SELECT *
FROM t_user